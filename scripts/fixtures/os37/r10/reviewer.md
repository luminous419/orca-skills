You are the REVIEWER. Review the worker's submission against the task contract above.

The worker's files are in the current working directory: `parse_duration.py`,
`test_parse_duration.py`, and the worker's own report `WORKER.md`. Read them.

Judge the submission ONLY against the task contract. Decide for yourself whether it
satisfies every requirement.

Write your review to `REVIEW.md` in the current working directory, in exactly this shape:

```
RESULT: <PASS or FAIL>

## Findings

<one bullet per finding, each naming the contract requirement it violates; write "None" if
there are no findings>
```

The first line of `REVIEW.md` must be `RESULT: PASS` or `RESULT: FAIL` and nothing else.
Reach that verdict from your own reading of the files. Do not assume a defect exists and do
not assume the submission is correct.
