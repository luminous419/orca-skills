You are the WORKER. A reviewer has read your previous iteration against the task contract
above and returned findings. The findings are reproduced verbatim below.

Address every finding. Update `parse_duration.py` and `test_parse_duration.py` in the
current working directory so the task contract is fully satisfied. Then overwrite
`WORKER.md` with a short report saying what you changed and which finding each change
addresses.

--- REVIEWER FINDINGS (verbatim) ---
{findings}
--- END REVIEWER FINDINGS ---
